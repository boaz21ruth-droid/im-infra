# Copy to .env.prod on EC2 and fill in real values
REGISTRY=yourname                    # Docker Hub username or ECR registry prefix
TAG=latest
DOMAIN=api.yourdomain.com            # Must point to EC2 public IP (Caddy auto-issues TLS)
POSTGRES_PASSWORD=change-me-strong   # im_business PostgreSQL password
